[project]
name = "my-engineering-notebook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs>=1.6.1",
    "mkdocs-shadcn>=0.0.1",
    "pygments==2.19.1",
]

[tool.vercel]
entrypoint = "app:app"
